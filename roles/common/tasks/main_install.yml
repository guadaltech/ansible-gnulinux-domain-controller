---
- name: Interface IP
  debug:
    msg: 
      - "Interface IP: {{ hostvars[groups['bind9'][0]].ansible_host }}"

- name: Create /etc/resolv.conf
  template:
    src: resolv.conf.j2
    dest: "/etc/resolv.conf"
